<?xml version="1.0" encoding="utf-8" ?>
<!--
  리뷰 매퍼 XML (벤치마크용)
  작성자: 최진호
  작성일: 2026-03-01
  수정일: 2026-03-04 - DB 스키마 일치
-->
<mapper namespace="BenchmarkNuVatis.Mappers.IReviewMapper">

  <!-- ResultMap 정의 (실제 DB 스키마에 맞춤) -->
  <resultMap id="ReviewResult" type="NuVatis.Benchmark.Core.Models.Review">
    <id column="id" property="Id" />
    <result column="user_id" property="UserId" />
    <result column="product_id" property="ProductId" />
    <result column="rating" property="Rating" />
    <result column="comment" property="Content" />
    <result column="created_at" property="CreatedAt" />
  </resultMap>

  <!-- Simple: GetById -->
  <select id="GetByIdAsync" resultMap="ReviewResult">
    SELECT id, user_id, product_id, rating, comment, created_at
    FROM reviews
    WHERE id = #{id}
  </select>

  <!-- Simple: WhereClause (상품별 리뷰) -->
  <select id="GetByProductIdAsync" resultMap="ReviewResult">
    SELECT id, user_id, product_id, rating, comment, created_at
    FROM reviews
    WHERE product_id = #{productId}
    ORDER BY created_at DESC
    LIMIT 100
  </select>

  <!-- Medium: TwoThreeJoin (Review + User + Product) -->
  <select id="GetWithDetailsAsync" resultType="NuVatis.Benchmark.Core.Models.Review">
    SELECT
        r.id, r.user_id, r.product_id, r.rating, r.comment AS Content, r.created_at
    FROM reviews r
    INNER JOIN users u ON r.user_id = u.id
    INNER JOIN products p ON r.product_id = p.id
    WHERE r.product_id = #{productId}
    ORDER BY r.created_at DESC
    LIMIT #{limit}
  </select>

  <!-- Medium: GroupByAggregate (상품별 평균 평점) -->
  <select id="GetAverageRatingByProductAsync" resultType="System.Collections.Generic.Dictionary`2[[System.Int64, System.Private.CoreLib],[System.Double, System.Private.CoreLib]]">
    SELECT
        product_id AS Key,
        AVG(rating::decimal) AS Value
    FROM reviews
    GROUP BY product_id
    HAVING COUNT(*) &gt;= 5
    ORDER BY Value DESC
  </select>

  <!-- Complex: WindowFunctions (리뷰별 순위) - DB 스키마에 맞춤 -->
  <select id="GetTopReviewsByProductAsync" resultType="dynamic">
    WITH ranked_reviews AS (
      SELECT
          id,
          product_id,
          user_id,
          rating,
          ROW_NUMBER() OVER (PARTITION BY product_id ORDER BY rating DESC, created_at DESC) AS rank
      FROM reviews
    )
    SELECT
        product_id,
        user_id,
        rating,
        rank
    FROM ranked_reviews
    WHERE rank &lt;= #{topN}
    ORDER BY product_id, rank
  </select>

  <!-- Simple: InsertSingle - DB 스키마에 맞춤 -->
  <insert id="InsertAsync">
    INSERT INTO reviews (user_id, product_id, rating, comment, created_at)
    VALUES (#{UserId}, #{ProductId}, #{Rating}, #{Content}, #{CreatedAt})
    RETURNING id
  </insert>

  <!-- Medium: BulkInsert1K - DB 스키마에 맞춤 -->
  <insert id="BulkInsertAsync">
    INSERT INTO reviews (user_id, product_id, rating, comment, created_at)
    VALUES
    <foreach collection="reviews" item="review" separator=",">
      (#{review.UserId}, #{review.ProductId}, #{review.Rating}, #{review.Content}, #{review.CreatedAt})
    </foreach>
  </insert>

</mapper>
